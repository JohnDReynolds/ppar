# Performance fixtures

These test-only CSVs anchor exact calculation values, frequency consolidation,
validation failures, and output-size checks. The user-facing demonstrations live in
`src/ppar/templates/`; add a fixture here only when it serves a focused automated
contract.
